# ConceptCrafter 計畫書

## 一、專案概述

**專案名稱：** ConceptCrafter - 概念圖製造機

**專案描述：** 一個無需伺服器的 HTML 網頁應用程式，用於創建、編輯和管理概念圖（Concept Map）。使用者可以直接在瀏覽器中運行，無需任何後端支持。

**技術棧：**
- HTML5
- CSS3
- JavaScript (原生，無框架依賴)
- Canvas API / SVG（用於圖形繪製）

---

## 二、核心功能需求

### 2.1 基本功能

| 功能編號 | 功能名稱 | 功能描述 | 優先級 |
|---------|---------|---------|-------|
| F-001 | 節點創建 | 在畫布上創建概念節點 | 必須 |
| F-002 | 節點編輯 | 修改節點文字內容 | 必須 |
| F-002a | 文字格式化 | 支援節點內容的粗體、斜體、底線等基本格式 | 必須 |
| F-002b | 節點超連結 | 在節點文字中指定超連結（可多個） | 必須 |
| F-002c | 任務完成標示 | 節點可標示為已完成（視覺上顯示✓符號） | 必須 |
| F-002d | 任務放棄標示 | 節點可標示為已放棄（視覺上顯示✗符號） | 必須 |
| F-003 | 節點刪除 | 移除不需要的節點 | 必須 |
| F-004 | 連接線創建 | 使用箭頭連接線連接兩個節點或框架 | 必須 |
| F-005 | 連接線標籤 | 為連接線添加關係描述文字 | 必須 |
| F-006 | 連接線刪除 | 移除不需要的連接線 | 必須 |
| F-006a | 箭頭樣式選擇 | 連接線支援單箭頭（預設）、雙箭頭和無箭頭 | 必須 |
| F-007 | 框架（Frame） | 創建集合框架，用於分組相關節點 | 必須 |
| F-007a | 框架連接 | 框架可以像節點一樣被連接線連結 | 必須 |
| F-008 | 拖拽移動 | 用滑鼠拖動節點和框架位置 | 必須 |
| F-009 | 放大縮小 | 支援畫布縮放功能 | 需要 |

### 2.2 檔案管理

| 功能編號 | 功能名稱 | 功能描述 | 優先級 |
|---------|---------|---------|-------|
| F-010 | 創建專案 | 使用資料夾選擇對話框建立新專案文件夾 | 必須 |
| F-011 | 打開專案 | 使用資料夾選擇對話框打開既存專案 | 必須 |
| F-012 | 專案結構 | 一個專案對應一個資料夾，一個 Map 對應一個 HTML 檔 | 必須 |
| F-013 | 新建 Map | 在當前項目中建立新的概念圖，指定 HTML 檔名和 Map 標題 | 必須 |
| F-014 | 打開 Map | 從項目中選擇已有的 HTML 檔案打開概念圖 | 必須 |
| F-015 | 保存 Map | 將當前概念圖保存為 HTML 檔到項目文件夾 | 必須 |
| F-016 | 導入 HTML | 從其他位置導入 HTML 檔案到項目 | 需要 |
| F-017 | 自動保存 | 提供本地存儲暫存功能（IndexedDB） | 需要 |

### 2.3 使用者介面

| 功能編號 | 功能名稱 | 功能描述 | 優先級 |
|---------|---------|---------|-------|
| F-018 | 工具欄 | 提供創建、編輯、刪除等工具按鈕 | 必須 |
| F-019 | 專案選擇器 | 顯示當前打開的專案和快速切換 | 必須 |
| F-020 | Map 列表 | 顯示當前專案中所有的 Map，支援切換 | 必須 |
| F-021 | 右鍵菜單 | 節點和連接線的快速操作菜單 | 需要 |
| F-022 | 屬性面板 | 顯示和修改選中對象的屬性 | 需要 |
| F-020 | 狀態欄 | 顯示當前操作狀態和幫助信息 | 需要 |
| F-021 | 變動追蹤 | 追蹤 Map 的變動狀態，未保存時顯示標記 | 必須 |

---

## 三、技術設計

### 3.1 資料結構

```javascript
// 節點對象
{
  id: string,                    // 唯一識別符
  type: "concept",              // 節點類型
  content: {                     // 節點內容
    text: string,               // 富文本內容 (支援 HTML 或 Markdown)
    html: string                // 渲染後的 HTML
  },
  x: number,                    // X 座標
  y: number,                    // Y 座標
  width: number,                // 寬度
  height: number,               // 高度
  hyperlinks: [                 // 超連結陣列
    {
      text: string,             // 連結文字
      url: string,              // 連結 URL
      startIndex: number,       // 文字中的起始位置
      endIndex: number          // 文字中的結束位置
    }
  ],
  status: {                     // 節點狀態
    isCompleted: boolean,       // 任務是否完成（預設: false）
    isAbandoned: boolean        // 是否放棄/廢棄（預設: false）
  },
  color: string,                // 節點顏色 (可選)
  fontSize: number              // 字體大小 (可選)
}

// 連接線對象
{
  id: string,                    // 唯一識別符
  type: "arrow",                // 連接線類型
  sourceId: string,             // 起始節點/框架 ID
  sourceType: string,           // 起始對象類型: "node" 或 "frame"
  targetId: string,             // 終止節點/框架 ID
  targetType: string,           // 終止對象類型: "node" 或 "frame"
  label: string,                // 連接線標籤文字
  arrowStyle: string,           // 箭頭樣式: "single"(預設)/雙/"none" 
  color: string,                // 連接線顏色 (可選)
  lineStyle: string             // 線條樣式: solid/dashed (可選)
}

// 框架（Frame）對象
{
  id: string,                    // 唯一識別符
  type: "frame",                // 框架類型
  label: string,                // 框架標籤
  x: number,                    // X 座標
  y: number,                    // Y 座標
  width: number,                // 寬度
  height: number,               // 高度
  nodeIds: string[],            // 包含的節點 IDs
  color: string,                // 框架顏色 (可選)
  opacity: number,              // 框架透明度 (可選)
  isConnectable: boolean        // 是否可連接 (預設為 true)
}

// 概念圖對象
{
  id: string,                    // 圖表唯一識別符
  filename: string,              // 對應的 HTML 檔名 (如: index.html, concept-01.html)
  title: string,                 // 圖表標題 (可與檔名不同，如: 首頁、主要概念等)
  version: string,              // 版本號
  nodes: Node[],                // 節點陣列
  arrows: Arrow[],              // 連接線陣列
  frames: Frame[],              // 框架陣列
  isDirty: boolean,             // 是否有未保存的變動（預設: false）
  createdAt: string,            // 創建時間
  updatedAt: string             // 更新時間
}
```

### 3.2 檔案結構

```
ConceptCrafter/
├── README.md
├── 計畫書.md
├── concept-crafter.html        # 主應用程式檔案
├── css/
│   └── style.css               # 樣式檔案
├── js/
│   ├── app.js                  # 主應用邏輯
│   ├── canvas-manager.js       # 畫布管理
│   ├── node-manager.js         # 節點管理
│   ├── arrow-manager.js        # 連接線管理
│   ├── frame-manager.js        # 框架管理
│   ├── file-manager.js         # 檔案管理
│   ├── ui-manager.js           # UI 管理
│   └── utils.js                # 工具函數
└── examples/                   # 示例項目
    └── example-project/        # 示例概念圖
        ├── project.json        # 項目配置檔
        ├── main.html           # 主概念圖
        ├── related-concepts.html
        └── ...

# 項目資料夾結構
my-project/                      # 用戶創建的項目資料夾
├── project.json               # 項目配置檔（項目名稱、創建時間等）
├── index.html                 # 對應 map 標題「首頁」
├── concept-architecture.html  # 對應 map 標題「系統架構」
├── concept-workflow.html      # 對應 map 標題「工作流程」
└── ...

# project.json 內容範例
{
  "projectId": "project-001",
  "projectName": "我的概念圖項目",
  "createdAt": "2025-11-27T12:00:00Z",
  "updatedAt": "2025-11-27T15:30:00Z",
  "maps": [
    {
      "id": "map-001",
      "filename": "index.html",
      "title": "首頁",
      "createdAt": "2025-11-27T12:00:00Z",
      "updatedAt": "2025-11-27T12:00:00Z"
    },
    {
      "id": "map-002",
      "filename": "concept-architecture.html",
      "title": "系統架構",
      "createdAt": "2025-11-27T13:00:00Z",
      "updatedAt": "2025-11-27T14:00:00Z"
    }
  ]
}
```

### 3.3 核心模組設計

#### 模組 1：Canvas Manager（畫布管理器）
- 負責初始化和管理 Canvas 元素
- 處理放大/縮小和平移
- 管理滑鼠事件（點擊、拖動、滾輪）
- 負責重繪所有元素（節點、連接線、框架、狀態符號）
- 根據節點狀態渲染對應的視覺指示符（✓ 或 ✗）
- 實現連接線標籤避開邏輯 - 連接線在標籤位置自動斷開，不遮蓋標籤

#### 模組 2：Node Manager（節點管理器）
- 創建、編輯、刪除節點
- 管理節點位置和大小
- 處理節點選擇和高亮
- 支援節點超連結設置
- 管理節點狀態（完成/放棄）並更新視覺表現
- 節點狀態變化時觸發重繪

#### 模組 3：Arrow Manager（連接線管理器）
- 創建、編輯、刪除連接線
- 支援連接線連接節點與節點、節點與框架、框架與框架
- 計算連接線路徑和箭頭方向
- **精確計算箭頭位置** - 箭頭尖端精確定位在目標節點/框架的邊線上，不被目標節點遮蓋
- 管理連接線標籤位置 - 標籤自動避開連接線，不被連接線遮蓋
- 檢測連接線與節點/框架的碰撞
- 自動計算框架邊界作為連接點
- 為標籤添加背景（可選），使其易於識別

#### 模組 4：Frame Manager（框架管理器）
- 創建、編輯、刪除框架
- 自動調整框架大小以容納內部節點
- 支援框架的拖拽移動
- 管理框架內節點的分組

#### 模組 5：File Manager（檔案管理器）
- 專案資料夾管理（創建、打開、切換）
- 延遲加載 Map 列表 - 打開專案時只讀取 project.json，不加載所有 Map 內容
- 按需加載 Map 數據 - 只有切換到某個 Map 時才讀取對應的 HTML 檔
- 變動追蹤 - 監控 Map 的變動並設置 isDirty 標誌
- 將概念圖序列化為 JSON 資料
- 導出為 HTML 檔案到專案文件夾
- 保存時清除 isDirty 標誌
- 從專案文件夾中的 HTML 檔案導入概念圖
- 搜索和篩選 Map 列表
- 本地 IndexedDB 存儲管理（暫存編輯）
- 專案路徑和檔案列表管理

#### 模組 6：UI Manager（UI 管理器）
- 管理工具欄按鈕狀態
- 顯示/隐藏屬性面板
- 處理右鍵菜單
- 更新狀態欄信息
- 管理 Map 列表的視覺狀態
- 追蹤並顯示 Map 的變動指示符（●或*）
- Map 保存後自動更新變動指示符

---

## 四、使用者介面設計

### 4.1 主介面佈局

```
┌─────────────────────────────────────────────────────┐
│  [新建] [打開] [保存] | 專案: /path/to/project       │ 工具欄
├────────┬─────────────────────────────────────────────┤
│ Map    │                                              │
│ 列表   │                                              │
│        │           Canvas 畫布區域                    │ 屬性
│ ┌─────┤          (概念圖繪製區域)                    │ 面板
│ │ 首頁 │                                              │
│ ├─────┤                                              │
│ │架構 │                                              │
│ ├─────┤                                              │
│ │流程 │                                              │
│ └─────┤                                              │
├────────┴─────────────────────────────────────────────┤
│ 狀態：已編輯 | 專案位置：/path/to/project            │ 狀態欄
└─────────────────────────────────────────────────────┘
```

### 4.4 工具欄按鈕

- **新建專案** - 使用資料夾選擇對話框建立新專案
- **打開專案** - 使用資料夾選擇對話框打開既存專案
- **新建 Map** - 在當前專案中新建概念圖（指定檔名和標題）
- **保存** - 將當前 Map 保存為 HTML 檔到專案文件夾
- **添加節點** - 在畫布上添加新節點
- **添加連接線** - 連接兩個節點或框架
- **添加框架** - 創建分組框架
- **刪除** - 刪除選中的對象
- **文字編輯** - 打開節點文字編輯器（支援格式化和超連結）
- **標示完成** - 將選中節點標示為已完成（顯示 ✓）
- **標示放棄** - 將選中節點標示為已放棄（顯示 ✗）
- **清除狀態** - 清除選中節點的完成/放棄狀態
- **清除全部** - 清空當前 Map 所有內容
- **說明** - 顯示應用程式使用說明和快捷鍵指南
- **幫助** - 顯示使用幫助

### 4.5 Map 列表面板

左側邊欄顯示當前專案中的 Map 列表，採用延遲加載策略：
- **首次打開專案** - 只讀取 project.json，不立即加載所有 Map 內容
- **列表顯示** - 顯示 Map 的標題和檔名（從 project.json）
- **變動指示符** - Map 有未保存變動時，在列表中顯示 `●`（圓點）或 `*` 標記
  - 已保存：無標記
  - 有變動：顯示 `●` 或 `*` 標記
  - 保存後自動消除標記
- **按需加載** - 只有當用戶點擊切換到某個 Map 時才讀取對應的 HTML 檔
- **搜索/篩選** - 支援在列表中搜索 Map 標題或檔名
- **虛擬滾動** - 若 Map 數量非常多，使用虛擬滾動以提高性能
- **右鍵菜單** - 支援快速操作：重命名、刪除、複製、導出等
- **當前 Map 突出** - 突出顯示當前打開的 Map

### 4.8 連接線箭頭設計

連接線箭頭的位置和計算方式設計如下：

| 特性 | 描述 |
|------|------|
| 尖端位置 | 箭頭三角形的尖端精確定位在目標節點/框架的邊線上 |
| 尖端不被遮蓋 | 箭頭尖端絕不被目標節點的背景色遮蓋 |
| 計算方法 | 根據連接線方向和目標節點形狀，精確計算交點 |
| 箭頭大小 | 根據連接線粗度自動調整箭頭大小 |
| 箭頭樣式 | 支援單箭頭、雙箭頭、無箭頭 |

**技術實現**
- 計算連接線方向向量
- 獲取目標節點的邊界盒（矩形）或框架的邊界
- 計算連接線與節點/框架邊線的交點
- 箭頭尖端位置 = 交點位置
- 箭頭三角形的兩條邊從尖端向後延伸，形成箭頭形狀
- 確保箭頭完全在節點外部，不被節點背景遮蓋
- 若節點內有填充顏色，調整箭頭顏色以增加對比度

### 4.9 連接線標籤設計

連接線標籤的位置和顯示方式設計如下：

| 特性 | 描述 |
|------|------|
| 標籤位置 | 位於連接線的中點，垂直偏移（向上或向下）以避開連接線 |
| 背景設計 | 標籤文字的背景為半透明白色或淡色，提高可讀性 |
| 避開連接線 | 連接線在標籤下方自動斷開，不穿過標籤 |
| 編輯方式 | 雙擊標籤可進行編輯 |
| 動態調整 | 當連接線改變時，標籤位置自動重新計算 |

**技術實現：**
- 計算連接線的中點和方向
- 根據連接線斜率，自動確定標籤應向上或向下偏移
- 標籤周圍添加 5-10px 的邊距
- 在繪製連接線時，在標籤所在區域斷開連接線，然後在標籤後繼續繪製
- 標籤文字使用清晰的字體和對比度高的顏色

### 4.10 節點編輯對話框
- 基本文字輸入和編輯
- **粗體**、*斜體*、<u>底線</u> 等格式化
- 超連結添加/編輯/刪除
- 文字顏色和字體大小設置
- **任務狀態設置** - 選擇節點狀態：正常、已完成、已放棄
- 實時預覽

### 4.12 說明對話框

點擊工具欄的「說明」按鈕打開說明對話框，包含以下內容：

#### 基本操作

| 操作 | 說明 |
|------|------|
| 新建節點 | 點擊「添加節點」按鈕，然後在畫布上點擊要放置的位置 |
| 編輯節點 | 雙擊節點打開編輯對話框，修改文字、格式和超連結 |
| 刪除節點 | 選中節點，點擊「刪除」按鈕或按 Delete 鍵 |
| 標示完成 | 選中節點，點擊「標示完成」按鈕，節點右上角顯示 ✓ |
| 標示放棄 | 選中節點，點擊「標示放棄」按鈕，節點右上角顯示 ✗ |

#### 連接線操作

| 操作 | 說明 |
|------|------|
| 創建連接線 | 點擊「添加連接線」按鈕，先點擊起始節點，再點擊終止節點 |
| 編輯連接線標籤 | 雙擊連接線標籤進行編輯 |
| 修改箭頭樣式 | 選中連接線，在屬性面板中選擇箭頭樣式（單箭頭/雙箭頭/無箭頭） |
| 刪除連接線 | 選中連接線，點擊「刪除」按鈕或按 Delete 鍵 |

#### 框架操作

| 操作 | 說明 |
|------|------|
| 創建框架 | 點擊「添加框架」按鈕，在畫布上拖拽創建框架 |
| 添加節點到框架 | 在框架建立後，將節點拖入框架內 |
| 框架連接 | 框架可以像節點一樣被連接線連結 |
| 刪除框架 | 選中框架，點擊「刪除」按鈕。框架內的節點不會被刪除 |

#### 項目和 Map 管理

| 操作 | 說明 |
|------|------|
| 新建項目 | 點擊「新建項目」按鈕，選擇資料夾位置或創建新資料夾 |
| 打開項目 | 點擊「打開項目」按鈕，選擇既存項目資料夾 |
| 新建 Map | 在項目打開時，點擊「新建 Map」按鈕，指定 HTML 檔名和 Map 標題 |
| 切換 Map | 在左側 Map 列表中點擊要打開的 Map |
| 保存 Map | 點擊「保存」按鈕，Map 會被保存為 HTML 檔到項目資料夾 |

#### 畫布操作

| 操作 | 說明 |
|------|------|
| 拖拽移動 | 按住滑鼠左鍵拖拽節點或框架移動位置 |
| 放大/縮小 | 使用滑鼠滾輪放大（向上滾）或縮小（向下滾）畫布 |
| 平移畫布 | 按住空格鍵並拖拽滑鼠平移畫布視圖 |
| 選擇對象 | 單擊節點、連接線或框架即可選擇 |
| 多選 | 按住 Ctrl（Cmd）鍵並點擊可多選多個對象 |

#### 快捷鍵

| 快捷鍵 | 功能 |
|-------|------|
| Ctrl+S / Cmd+S | 保存當前 Map |
| Ctrl+Z / Cmd+Z | 撤銷上一步操作 |
| Ctrl+Y / Cmd+Shift+Z | 重做操作 |
| Delete | 刪除選中的對象 |
| Ctrl+A / Cmd+A | 全選所有對象 |
| Ctrl+C / Cmd+C | 複製選中的對象 |
| Ctrl+V / Cmd+V | 粘貼對象 |
| Double-Click | 編輯節點或連接線標籤 |

#### 視覺標記說明

| 標記 | 含義 |
|------|------|
| ● 或 * | Map 列表中表示該 Map 有未保存的變動 |
| ✓（綠色） | 節點右上角表示該任務已完成 |
| ✗（紅色） | 節點右上角表示該任務已放棄 |
| 藍色邊框 | 表示對象被選中 |

#### 常見問題

**Q: 如何調整節點的大小？**  
A: 選中節點後，在屬性面板中可以修改節點的寬度和高度。

**Q: 如何改變節點的顏色？**  
A: 選中節點，在屬性面板中選擇顏色，或在節點編輯對話框中修改。

**Q: 可以在一個 Map 中放多少個節點？**  
A: 理論上沒有限制，但為了性能考慮，建議單個 Map 不超過 500 個節點。

**Q: 導出的 HTML 檔案可以在其他電腦上打開嗎？**  
A: 可以，導出的 HTML 檔案是完全獨立的，可以在任何支援現代瀏覽器的電腦上打開。

**Q: 如何分享我的概念圖？**  
A: 直接分享導出的 HTML 檔案即可。接收方可以在瀏覽器中打開查看和編輯。

### 4.13 節點視覺表現

節點的背景會根據狀態顯示不同的指示符：

| 狀態 | 視覺表現 | 描述 |
|------|--------|------|
| 正常 | 普通背景 | 無任何符號 |
| 已完成 | ✓（綠色勾選） | 在節點右上角顯示綠色的 ✓ 符號，表示任務已完成 |
| 已放棄 | ✗（紅色叉叉） | 在節點右上角顯示紅色的 ✗ 符號，表示任務已放棄 |
| 已完成 + 已放棄 | 優先顯示 ✗ | 若節點同時被標記為完成和放棄，優先顯示 ✗ |

**設計細節：**
- 符號位置：節點的右上角，距離邊緣 5-8px
- 符號大小：節點高度的 30-40%
- 勾選符號色：綠色 (#00AA00 或類似)
- 叉叉符號色：紅色 (#FF0000 或類似)
- 符號背景：可選半透明白色背景以提高可見性

---

## 五、開發階段和時程

### 第一階段：基礎框架搭建（第 1-2 周）
- [ ] 創建 HTML 基本結構
- [ ] 設置 Canvas 畫布和事件系統
- [ ] 實現基本的 UI 框架

### 第二階段：核心功能開發（第 3-4 周）
- [ ] 實現節點的創建、編輯、刪除
- [ ] 實現連接線的創建、編輯、刪除
- [ ] 實現滑鼠拖拽功能

### 第三階段：進階功能開發（第 5-6 周）
- [ ] 實現框架（Frame）功能
- [ ] 實現節點超連結功能
- [ ] 實現放大/縮小功能

### 第四階段：檔案管理和導出（第 7-8 周）
- [ ] 實現數據序列化
- [ ] 實現 HTML 導出功能
- [ ] 實現 HTML 導入功能
- [ ] 實現本地存儲功能

### 第五階段：UI 優化和測試（第 9-10 周）
- [ ] 美化界面設計
- [ ] 添加右鍵菜單和屬性面板
- [ ] 功能測試和 Bug 修復
- [ ] 性能優化

### 第六階段：文檔和示例（第 11 周）
- [ ] 編寫用戶使用手冊
- [ ] 編寫開發者文檔
- [ ] 創建示例項目
- [ ] 最終測試和發布

---

## 六、技術實現要點

### 6.1 繪圖技術選型
- **Canvas API** - 用於繪製節點、連接線和框架
- 優點：性能好，適合實時交互
- 缺點：需要手動管理圖形

### 6.2 Map 延遲加載策略
- **專案打開流程**：
  1. 用戶打開專案資料夾
  2. 應用讀取 project.json 獲取 Map 列表元數據
  3. Map 列表面板顯示所有 Map 的標題和檔名
  4. 只在用戶點擊切換 Map 時，才讀取對應的 HTML 檔並載入內容
- **優點**：啟動快速，記憶體使用低，支援大量 Map
- **搜索優化**：搜索時只搜索 project.json 中的元數據，無需載入所有 HTML

### 6.3 變動追蹤機制
- **追蹤時機**：任何編輯操作（添加/編輯/刪除節點、連接線、框架等）都會將 `isDirty` 設置為 `true`
- **視覺反饋**：Map 列表中 isDirty 為 true 的 Map 顯示 `●` 或 `*` 標記
- **保存流程**：
  1. 用戶點擊保存按鈕
  2. 應用序列化 Map 數據並生成 HTML 檔
  3. 保存到項目文件夾
  4. 將 `isDirty` 設置為 `false`
  5. 更新 Map 列表，移除標記
- **自動保存**（可選）：支援定時自動保存到 IndexedDB，以防數據丟失

### 6.4 數據持久化
- 使用 JSON 格式存儲概念圖數據
- 嵌入到 HTML 檔案中的 `<script>` 標籤
- 使用 Base64 編碼保存二進制數據（如果需要）

### 6.5 跨瀏覽器相容性
- 支援 Chrome、Firefox、Safari、Edge（最新版本）
- 使用標準 Web APIs，避免私有擴展

### 6.6 無伺服器執行
- 所有邏輯在瀏覽器中執行
- 使用 File API 處理文件上傳和下載
- 使用 LocalStorage 進行數據暫存

---

## 七、相關資源和參考

### 參考技術
- MDN Web Docs - Canvas API
- SVG 圖形技術
- File API 和 Blob 操作
- LocalStorage 和 IndexedDB

### 示例應用
- Draw.io（開源圖形編輯器）
- Lucidchart（在線圖表工具）
- OmniGraffle（概念圖工具）

---

## 八、成功標準

✅ 應用程式可以直接在瀏覽器中運行，無需伺服器
✅ 支援創建、編輯、刪除節點
✅ 支援帶箭頭的連接線
✅ 節點支援超連結功能
✅ 支援框架分組功能
✅ 可以導出為獨立的 HTML 檔案
✅ 支援項目文件夾管理
✅ UI 直觀易用，操作流暢

---

## 九、風險評估與應對

| 風險 | 影響 | 概率 | 應對策略 |
|-----|------|------|---------|
| 性能問題（大量節點） | 應用卡頓 | 中 | 使用虛擬化技術，分批渲染 |
| 跨瀏覽器相容性問題 | 某些瀏覽器無法使用 | 低 | 充分測試，使用 polyfill |
| 複雜圖形邏輯困難 | 開發周期延長 | 中 | 提前進行技術原型驗證 |
| 數據損失風險 | 用戶數據丟失 | 低 | 實現自動保存和匯出功能 |

---

**文檔版本：** v1.0
**最後更新：** 2025年11月27日
