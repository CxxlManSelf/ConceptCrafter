# Concept Crafter - 專案管理說明

## 資料存放位置

### 1. 暫存資料（瀏覽器記憶體）
- **位置**：瀏覽器的 localStorage
- **內容**：專案名稱和各專案的 Maps 資料
- **用途**：編輯時的暫存，切換專案時保留資料

### 2. 永久儲存（檔案系統）
- **位置**：您手動建立的專案資料夾
- **內容**：匯出的 HTML 檔案
- **用途**：長期保存、備份、分享

## 建議的專案資料夾結構

```
C:\MySrc\ConceptCrafter\
├── concept-crafter.html          (主程式)
├── 專案管理說明.md               (本說明文件)
└── projects\                      (所有專案的根目錄)
    ├── 專案A\                     (專案 A 的資料夾)
    │   ├── map1.html
    │   ├── map2.html
    │   └── map3.html
    ├── 專案B\                     (專案 B 的資料夾)
    │   ├── 概念圖1.html
    │   └── 概念圖2.html
    └── 專案C\                     (專案 C 的資料夾)
        └── architecture.html
```

## 工作流程

### 開始新專案

1. **在檔案總管中建立專案資料夾**
   ```
   C:\MySrc\ConceptCrafter\projects\我的新專案\
   ```

2. **在 Concept Crafter 中**
   - 點擊「切換專案」按鈕
   - 輸入專案名稱：`我的新專案`
   - 在「Map 名稱」欄位輸入第一個 map 的名稱

3. **建立內容**
   - 新增節點、連接線、框架
   - 點擊「儲存 Map」（暫存到瀏覽器）

4. **匯出 HTML 檔案**
   - 點擊「另存為 HTML」
   - **重要**：在下載對話框中，瀏覽並選擇您建立的專案資料夾
   - 檔案會以 Map 名稱命名並下載

### 切換專案

1. **方式一：使用「切換專案」按鈕**
   - 點擊「切換專案」
   - 輸入專案名稱
   - 系統會自動載入該專案的所有 Maps（從 localStorage）

2. **方式二：從檔案載入**
   - 點擊「從檔案載入」
   - 選擇專案資料夾中的任一 HTML 檔案
   - 系統會載入該 Map 和專案資訊

### 在同一專案中建立多個 Maps

1. 確認「當前專案」欄位顯示正確的專案名稱
2. 點擊「新增 Map」
3. 輸入新 Map 名稱
4. 建立內容
5. 點擊「儲存 Map」
6. 點擊「另存為 HTML」，選擇同一個專案資料夾

### 載入已存在的專案

**如果之前有在瀏覽器中編輯過：**
- 點擊「切換專案」
- 輸入專案名稱
- 左側會顯示所有儲存在 localStorage 的 Maps

**如果是全新瀏覽器或清除過快取：**
- 點擊「從檔案載入」
- 選擇專案資料夾中的 HTML 檔案
- 每載入一個 HTML 檔案，該 Map 就會加入到當前專案

## 重要提醒

### 資料安全

1. **定期匯出 HTML**
   - localStorage 可能因清除瀏覽器快取而遺失
   - 建議完成重要修改後立即「另存為 HTML」

2. **備份專案資料夾**
   - 定期將整個 projects 資料夾備份到雲端或外接硬碟

3. **版本控制**
   - 可以使用 Git 管理專案資料夾
   - 每個 HTML 檔案都是獨立完整的，適合版本控制

### 瀏覽器限制

- 由於瀏覽器安全限制，程式**無法自動建立資料夾**
- 下載時需要**手動選擇**要儲存的專案資料夾
- 這是所有網頁應用程式的共同限制

## 常見問題

**Q: 我的專案資料存在哪裡？**
A:
- 編輯中的資料：瀏覽器的 localStorage（按 F12 → Application → Local Storage 可查看）
- 永久儲存：您手動建立的專案資料夾中的 HTML 檔案

**Q: 如何完整備份一個專案？**
A:
1. 確保所有 Maps 都已「另存為 HTML」
2. 複製整個專案資料夾到備份位置

**Q: 清除瀏覽器快取會影響嗎？**
A:
- 會：localStorage 中的暫存資料會遺失
- 不會：已匯出的 HTML 檔案不受影響
- 解決方式：使用「從檔案載入」重新載入 HTML 檔案

**Q: 可以在不同電腦間共用專案嗎？**
A:
- 可以！將專案資料夾（含所有 HTML 檔案）複製到另一台電腦
- 使用「從檔案載入」載入 HTML 檔案即可

**Q: 如何刪除不要的專案？**
A:
- localStorage：點擊「切換專案」切換到該專案，手動刪除 Maps，或使用瀏覽器開發工具清除
- 檔案系統：直接在檔案總管中刪除專案資料夾

## 快速參考

| 動作 | 按鈕/方法 | 結果 |
|------|----------|------|
| 建立新專案 | 「切換專案」→ 輸入新名稱 | 切換到新專案，清空畫布 |
| 建立新 Map | 「新增 Map」 | 在當前專案建立新 Map |
| 暫存 Map | 「儲存 Map」 | 儲存到 localStorage |
| 永久儲存 | 「另存為 HTML」 | 下載 HTML 檔案 |
| 載入 Map | 「從檔案載入」 | 從 HTML 檔案載入 |
| 切換專案 | 「切換專案」 | 切換到其他專案 |
| 查看專案的 Maps | - | 左側「Map 列表」 |
